{
  "name": "megaNote",
  "private": true,
  "version": "0.5.0",
  "description": "A note-taking app.",
  "repository": "git@github.com/tworns/megaNote.git",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "start": "http-server app -a localhost -p 8000 -c-1",
    "prestart": "npm install",
    "postinstall": "bower install",
    "preupdate-webdriver": "npm install"
  }
}
